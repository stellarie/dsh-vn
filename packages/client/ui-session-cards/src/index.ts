/** Collapsed-Sidebar session cards, node half: this feature is browser-only. */

/** Host plugin body — the cards are a Web presentation concern and mount nothing here. */
export function apply(): void {}
