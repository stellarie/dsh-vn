# Import provenance

Imported from `stellarie/imouto-dev` revision `79a76eed37ce21f9406051b0bc46c53db931e3dd`.

- `hosts/dsh/persona.md` supplies the Yuu persona.
- `hosts/dsh/agent.cordis.yml` supplies the preset composition.
- `canon/subimouto-dev.md` supplies `subimouto-dev`.
- `canon/imouto-agent-team.md` supplies `imouto-agent-team`.
- `canon/deepseek-only-dev.md` supplies `deepseek-only-dev`.

The repository owner authorized this import into `stellarie/dsh-vn`.

## Local repairs

The three skill bodies are the canon text with these reference repairs:

- `ste-writing` becomes `writing-ste100`. The unshipped `notes-wiki` and `tool-creation` routes are removed.
- The bare protocol and reasoning-guide references now name the `imouto-blackboard` and `how-claude-thinks` skills.
- `PROTOCOL.md` becomes `imouto-blackboard`.
